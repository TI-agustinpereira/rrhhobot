# deps  de consulta

fastapi==0.136.3
uvicorn[standard]==0.48.0
openai==2.39.0
psycopg2-binary==2.9.12
python-dotenv==1.2.2
pydantic==2.13.4

# El stack de Teams (botbuilder/botframework) NO se incluye: la web no usa
# /api/messages. main.py importa Teams de forma opcional (try/except), asi que
# la app levanta igual sin estas deps. Para correr Teams en local, instalalas aparte.
